[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "poppler_utils", "tesseract"]

[phases.install]
cmds = [
  "python3.11 -m venv /app/venv",
  "/app/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "/app/venv/bin/python -m uvicorn api:app --host 0.0.0.0 --port $PORT"
